VITE_SUPABASE_URL=https://<project-id>.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=<anon-key>
VITE_SUPABASE_PROJECT_ID=<project-id>

# PostHog product analytics (public project key — like the Supabase anon key).
# Optional: leave unset and PostHog stays fully dark (the integration no-ops).
# Analytics/session-replay/error-tracking are each gated by the user's cookie
# consent, so nothing is captured until a visitor opts in. Host defaults to EU
# Cloud; override only for US Cloud (https://us.i.posthog.com) or self-hosted.
# VITE_POSTHOG_KEY=phc_<project-api-key>
# VITE_POSTHOG_HOST=https://eu.i.posthog.com

# Server-only production build configuration for PostHog source-map uploads.
# Never prefix these with VITE_ or commit their values. The management host must
# match the region of VITE_POSTHOG_HOST (EU: eu.posthog.com / eu.i.posthog.com).
# Uploads are skipped when any of these values is absent.
# POSTHOG_API_KEY=phx_<personal-api-key>
# POSTHOG_PROJECT_ID=246246
# POSTHOG_HOST=https://eu.posthog.com

# Local dev only — auto sign-in so gated routes skip the login screen while you
# run `npm run dev`. Uncomment and set to a real account for THIS project's
# Supabase. The auto-login code is guarded by `import.meta.env.DEV`, so it is
# stripped from production builds and can never run on a deployed server; keep
# these unset in any hosting provider. Credentials stay in your gitignored .env.
# VITE_DEV_AUTOLOGIN=true
# VITE_DEV_AUTOLOGIN_EMAIL=dev-admin@example.com
# VITE_DEV_AUTOLOGIN_PASSWORD=your-dev-password
