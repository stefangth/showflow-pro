# Part 1 — Bug Log

Findings surfaced by deep edge-function tests. Severity: CRITICAL (data loss / security / silent failure) · HIGH (wrong result for users) · MED · LOW.

| Function | Severity | Contract violated (app-logic.md ref) | Resolution | Test |
|---|---|---|---|---|
| `send-offer-digest` | MED | at-least-once email on stamp failure — if `sendEmail` succeeds but the subsequent `bookings.update` (stamp) returns an error, the handler logs and continues without incrementing `digests_sent`. The booking retains `digest_sent_at = null` and will be re-queried on the next run, sending a duplicate offer email. The idempotency key `offer-digest-<artistId>-YYYY-MM-DDTHH` mitigates duplicates within the same UTC hour only; a stamp failure at 19:01 and a retry at the next day's 19:00 run will produce an unavoidable duplicate (different key). Doc ref: _The Offer → Booking Flow_ — "Stamps `digest_sent_at = now()` … offers will re-send next run". | design-limitation (acceptable at current scale; at-least-once delivery is the stated behavior); no code change. The harness cannot selectively fail the UPDATE without also failing the SELECT on the same table — a `// NOTE:` comment in the test documents this limitation and the verdict. | `send-offer-digest: atomicity characterization — harness limitation for stamp-failure path` |
| `expire-offers` | LOW (doc-gap) | Spec says "dedupe duplicate ids" for recipients but the handler does no deduplication. If `resolve_show_assignments` returns the same `producer_user_id` twice (possible if the RPC has its own join explosion), two `cast_escalation_requested` notifications are inserted for the same user and two emails are sent. The `send-offer-digest` handler (and `tier-at-risk-watcher`) both dedupe explicitly; `expire-offers` does not. | FIXED: added `[...new Set(recipientIds)]` for consistency with `tier-at-risk-watcher` | `expire-offers: dedupes duplicate producer ids → one notification + one email per user` |
| `tier-at-risk-watcher` | HIGH | Stale `tier_at_risk` notifications were never deleted when all open tiers closed. The handler's early-exit at `if (!openTiers || openTiers.length === 0) return json({ at_risk_count: 0 })` ran before the notification-cleanup block, so if a tier closed (got `closed_at` set) while its notification still existed, that notification persisted forever — leaving a stale "Tier at risk" warning in the UI indefinitely. Contract: _"clears when math recovers (by deleting the old row)"_ — a closed tier is definitively recovered. | FIXED: moved `notifications` query before the early-exit decision; added a combined guard `if (no open tiers AND no existing notifs)` for the true nothing-to-do case; recovery delete now runs regardless of open-tier count. Regression test: `tier-at-risk-watcher DI: stale notifications for closed tiers ARE cleared`. | `tier-at-risk-watcher DI: stale notifications for closed tiers ARE cleared (regression: was a bug)` |
| `send-transactional-email` | NONE (all contracts confirmed) | Suppression fail-closed: CONFIRMED. Suppression DB error fail-closed: CONFIRMED (handler returns 500 and does NOT call Resend). Snake_case body keys: CONFIRMED. Log non-blocking: CONFIRMED (log insert errors ignored on success path). Used-token safety fallback: CHARACTERIZATION — when `email_unsubscribe_tokens.used_at` is set but email is not in `suppressed_emails`, the handler returns `{ success: false, reason: 'email_suppressed' }` (safety fallback, line ~131). This is intentional-but-undocumented: it assumes a used token implies the user unsubscribed before the suppression row was written; blocks the send as a conservative safety measure. | No fix needed. Characterization test added with `// characterization:` comment. | `characterization: used token + not suppressed → returns { success: false, reason: 'email_suppressed' }` |
| `handle-email-unsubscribe` | HIGH | POST with an already-used token returned `{ valid: false, reason: 'already_unsubscribed' }` instead of the documented `{ success: false, reason: 'already_unsubscribed' }`. Root cause: the pre-check at line 70 used the GET-response key (`valid`) for both methods. The response body key `valid` vs `success` is load-bearing — the app's unsubscribe page distinguishes GET (validation) from POST (action) responses by checking these keys. A POST that hits the pre-check would have appeared as an unknown/truthy response to a caller expecting `success: false`. | FIXED: refactored the pre-check block so GET and POST each own their `used_at` guard, returning `{ valid: false }` and `{ success: false }` respectively. | `POST already-used token → { success: false, reason: 'already_unsubscribed' }` |
| `airtable-poll` | NONE (all contracts confirmed) | Auth (missing/wrong cron-secret → 401): CONFIRMED. Sync-disabled path: CONFIRMED. Missing base/table config: CONFIRMED. Invalid base_id format → 422: CONFIRMED. Missing AIRTABLE_API_KEY → 500: CONFIRMED. Fetch URL shape (base/encoded-table?view=Grid%20view) and Authorization: Bearer header: CONFIRMED. Field mapping (Date/date/Show Date variants, Show/show, City, Session 1/session_1/Start Time): CONFIRMED. Session defaults to 00:00 when absent: CONFIRMED. City null when unresolved: CONFIRMED. Show resolution by (program, sub_program) exact match then program-only fallback: CONFIRMED. Unresolvable show → `skipped` count incremented: CONFIRMED. New-date → insert + `invokeFunction('open-offer-tier', { show_date_id, tier: 1 })`: CONFIRMED. Existing-date → update only, no invokeFunction: CONFIRMED. Batch resilience (Promise.allSettled — one rejection → others succeed, still 200, tiers_opened = N-1): CONFIRMED. Response shape `{ processed, new_dates, tiers_opened, skipped }`: CONFIRMED. airtable_sync_log written with correct status/synced_at on success and on Airtable API error: CONFIRMED. | No bugs found; no handler changes required. | 26 tests in `index.di.test.ts` |
| `airtable-poll` | COVERAGE-GAP-01 | Multi-page Airtable pagination (`offset` in response → subsequent fetch with `&offset=<encoded>`) is not tested. The loop is a plain `do { fetch; offset = data.offset } while (offset && pageCount < MAX_PAGES)` and the MAX_PAGES=100 truncation path is also untested. A full multi-page test requires chaining multiple `fetchImpl` responses (first returns `{ records: [...], offset: "abc" }`, second returns `{ records: [...] }` with no offset). Scope was deferred as per plan; no bug risk identified in the loop structure itself. | Intentionally untested in this pass; tag for future improvement. | — |
| `_shared/testing.ts` | HIGH (harness) | `createFakeClient` did not apply `.in()` filtering when resolving seeds for `maybeSingle()`/`single()` calls. Consequence: `requireRole` (in `_shared/auth.ts`) calls `.in("role", ["admin"])` to restrict role lookup to allowed roles, but the fake client ignored that filter — returning any seeded `user_roles` row regardless of its `role` value. A non-admin user (e.g. `{ role: 'producer' }`) would pass an admin-only gate in tests. Discovered by the new `admin-decide-approval` DI test "producer role (not admin) → 403". | FIXED: tracked `.in()` args in the builder's `localIn` map; applied them as a row-membership filter inside `single()`/`maybeSingle()` resolution only (not in `.then()` list resolution, where simplified seed rows may omit the filtered column). Full suite: 379/379 pass, 0 fail. | `admin-decide-approval DI: producer role (not admin) → 403` |
| `admin-decide-approval` | NONE (all contracts confirmed) | No-auth → 401: CONFIRMED. Non-admin role → 403: CONFIRMED (after harness fix). Approval not found → 404: CONFIRMED. Missing fields → 400: CONFIRMED. Invalid decision → 400: CONFIRMED. Invalid role → 400: CONFIRMED. Approved path: `decide_user_approval` RPC called with correct `p_approval_id`, `p_decision`, `p_role`, `p_rejection_reason`, `p_decided_by` (caller's user id): CONFIRMED. Returns `{ ok: true }`: CONFIRMED. Email: `signup-decision` template, `recipient_email`, `idempotency_key` containing approval-id + decision, `templateData.decision`, `templateData.role` (approved only), `templateData.displayName`: CONFIRMED. `display_name` fallback to email: CONFIRMED. Rejected path: `p_decision='rejected'`, `p_rejection_reason` forwarded, email with `tData.decision='rejected'`, `tData.reason`, `tData.role=undefined`: CONFIRMED. Email failure non-blocking (handler returns 200 when `sendEmail` throws): CONFIRMED. RPC error → 500: CONFIRMED. | No handler bugs found. | 29 tests in `index.di.test.ts` |
| `_shared/testing.ts` | HIGH (harness) | `resolveSeed` / `.then()` path returned `{ data, error }` but never included `count`. Handlers using `select("*", { count:"exact", head:true })` destructure `{ count }` from the resolved value — receiving `undefined`. In `admin-set-role` the last-admin guard checks `(count ?? 0) <= 1`, which with `count=undefined` evaluates to `0 <= 1 = true` always, permanently blocking any `action=remove` with `role=admin` regardless of how many admins exist. Any future handler using Supabase count queries would have the same blind spot in tests. | FIXED: added optional `count?: number` to `SingleSeed` type; `resolveSeed` spreads `count` into the result only when explicitly set in the seed (omits the key when undefined to preserve backward-compatible deep-equality checks). Full suite: 392/392 pass. | `admin-set-role DI: prevent-last-admin: 2 admins exist → remove allowed → 200` (regression guard) |
| `admin-set-role` | NONE (all contracts confirmed, after harness fix) | No-auth → 401: CONFIRMED. Non-admin role → 403: CONFIRMED. Missing `user_id` → 400: CONFIRMED. Missing `role` → 400: CONFIRMED. Missing `action` → 400: CONFIRMED. Invalid action value → 400: CONFIRMED. Invalid role value → 400: CONFIRMED. `action=add`: insert recorded with `{ user_id, role }` payload → 200 `{ ok: true }`: CONFIRMED. Unique-violation error swallowed (`/duplicate|unique/i`) → idempotent 200: CONFIRMED. Non-unique insert error bubbles → 500: CONFIRMED. `action=remove` (non-admin role): delete recorded with `.eq("user_id", ...)` and `.eq("role", ...)` filters → 200: CONFIRMED. Prevent-last-admin guard (1 admin → blocked → 400 "Cannot remove the last admin"): CONFIRMED. Guard allows removal when 2 admins exist → 200 (post-harness-fix, was always-blocked before): CONFIRMED. | No handler bugs found; the real bug was in the test harness (see row above). | 13 tests in `index.di.test.ts` |
| `admin-list-users` | NONE (all contracts confirmed) | No-auth → 401: CONFIRMED. Bearer without "Bearer " prefix → 401: CONFIRMED. Role=producer → 403: CONFIRMED. Role=artist → 403: CONFIRMED. No role row → 403: CONFIRMED. Admin → 200 `{ users: [...] }`: CONFIRMED. User shape fields (`id`, `email`, `created_at`, `last_sign_in_at`, `roles[]`, `approval_status`): CONFIRMED — exact key set asserted. User without role row → `roles: []`: CONFIRMED. User without approval row → `approval_status: null`: CONFIRMED. User with multiple roles → all in `roles[]`: CONFIRMED. Approval status values (pending/approved/rejected): CONFIRMED. Empty auth store → 200 `{ users: [] }`: CONFIRMED. Pagination characterization: handler calls `listUsers({ page: 1, perPage: 1000 })` but the fake harness ignores pagination args entirely — production pagination behavior not exercised; noted with `// characterization:` comment. | No bugs found; no handler changes required. | 14 tests in `index.di.test.ts` |
| `preview-transactional-email` | NONE (all contracts confirmed) | No-auth → 401: CONFIRMED. Bearer without "Bearer " prefix → 401: CONFIRMED. Artist role → 403: CONFIRMED. No role row → 403: CONFIRMED. Admin → 200: CONFIRMED. Producer → 200 (both admin AND producer allowed): CONFIRMED. No templateName → `{ templates: [...] }` with exactly `Object.keys(TEMPLATES).length` (5) entries: CONFIRMED. Each entry has documented shape keys (templateName, displayName, subject, html, status): CONFIRMED. All 5 registry templates have previewData → all return `status: 'ready'` with non-empty html and non-empty subject: CONFIRMED. String subject resolved literally: CONFIRMED (`artist-confirmation-digest`). Function subject called with previewData: CONFIRMED (`signup-decision` → "You're approved…", `cast-escalation-requested` → uses previewData.program/date/tier). Known templateName → array of length 1, status='ready', html non-empty: CONFIRMED (each of the 5 templates individually). Unknown templateName → 200 (not a 4xx/5xx), status='render_failed', errorMessage contains template name, html='', subject='': CONFIRMED. Per-template try/catch confirmed: an unknown name does NOT abort the request; catch block pushes render_failed entry and response is still returned — structural coverage via unknown-name path; injecting a throwing React component would require module-level patching (coverage gap noted in test comments). Subject override (non-empty string) replaces subject: CONFIRMED. Whitespace-only subject override silently ignored (original kept): CONFIRMED. Non-string subject override silently ignored: CONFIRMED. Non-object overrides field silently ignored (no crash): CONFIRMED. Intro override applied to render (no crash, html non-empty): CONFIRMED. GET method with auth → renders all templates (body parsing skipped): CONFIRMED. | No bugs found; no handler changes required. | 38 tests in `index.di.test.ts` |
| `preview-transactional-email` | COVERAGE-GAP-01 (doc-gap) | Render-resilience for a React component that throws mid-render cannot be exercised without patching `TEMPLATES` at the module level (replacing a component entry with a function that throws). The per-template try/catch at index.ts:69-107 is confirmed to exist by code reading; the unknown-template path exercises the catch block's `render_failed` + `errorMessage` output. A full injection test would require either a test-only "bad" template entry or dynamic module patching, which Deno's native test runner does not support trivially. | Intentionally untested in this pass; tag for future improvement. | — |
