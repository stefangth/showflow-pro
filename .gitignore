# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
coverage

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files — never commit secrets
.env
.env.*
!.env.example

# Supabase CLI cache
supabase/.temp/

# Visual brainstorming companion (superpowers)
.superpowers/

# Dev-only visual-regression tooling for the Show Date Cockpit.
# public/ref is a generated reference render (regeneration is documented in the
# header of scripts/shoot-cockpit.mjs); it must never ship in the public/ build.
public/ref/
screenshots/
scratchpad_peek.png
